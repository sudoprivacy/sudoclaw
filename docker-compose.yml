services:
  sudoclaw-gateway:
    image: ${SUDOCLAW_IMAGE:-sudoclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      SUDOCLAW_GATEWAY_TOKEN: ${SUDOCLAW_GATEWAY_TOKEN}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
    volumes:
      - ${SUDOCLAW_CONFIG_DIR}:/home/node/.sudoclaw
      - ${SUDOCLAW_WORKSPACE_DIR}:/home/node/.sudoclaw/workspace
    ports:
      - "${SUDOCLAW_GATEWAY_PORT:-18789}:18789"
      - "${SUDOCLAW_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${SUDOCLAW_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]

  sudoclaw-cli:
    image: ${SUDOCLAW_IMAGE:-sudoclaw:local}
    network_mode: "service:sudoclaw-gateway"
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    environment:
      HOME: /home/node
      TERM: xterm-256color
      SUDOCLAW_GATEWAY_TOKEN: ${SUDOCLAW_GATEWAY_TOKEN}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
    volumes:
      - ${SUDOCLAW_CONFIG_DIR}:/home/node/.sudoclaw
      - ${SUDOCLAW_WORKSPACE_DIR}:/home/node/.sudoclaw/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
    depends_on:
      - sudoclaw-gateway
