---
summary: "CLI reference for `sudoclaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `sudoclaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `sudoclaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `sudoclaw clawbot qr` (same behavior as [`sudoclaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `sudoclaw clawbot qr` -> `sudoclaw qr`
