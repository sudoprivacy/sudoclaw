---
summary: "CLI reference for `sudoclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `sudoclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
sudoclaw uninstall
sudoclaw uninstall --all --yes
sudoclaw uninstall --dry-run
```
